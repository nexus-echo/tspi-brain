# DEPRECATED — merged into a single sample file.
# Use .env.example (it now covers both local and docker; see the [local]/[docker] notes inside).
#
#   cp .env.example .env
#
# This file is kept only to avoid breaking old links and can be deleted (git rm .env.docker.example).
